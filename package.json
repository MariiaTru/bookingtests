{
  "name": "booking",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "tests": "playwright test --config=playwright.config.ts",
    "tests:headed": "playwright test --config=playwright.config.ts --headed",
    "tests:api": "playwright test --config=api.config.ts"
  },
  "devDependencies": {
    "allure-playwright": "^3.2.1",
    "typescript": "^5.5.3"
  },
  "private": true,
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@playwright/test": "^1.51.1",
    "prettier": "^3.5.3"
  }
}
